function getValue(e) {
    s += document.getElementById(e).innerHTML;
    result.innerHTML = s;
    // If len(s) > 14, create substring for display
};

function compute(e) {
    arr += s;
    s = '';
    arr += document.getElementById(e).innerHTML;
};

function solve() {};